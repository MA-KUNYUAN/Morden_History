<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mordenhistory">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ActualQuiz"></activity>
        <activity android:name=".StartQuiz" />
        <activity android:name=".PdfView" />
        <activity android:name=".PdfList" />
        <activity android:name=".DetailYoutube" />
        <activity android:name=".YouTubeList" />
        <activity android:name=".Leaderboard" />
        <activity android:name=".Settings" />
        <activity android:name=".Detail_Option" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>